import Button from "@/component/ui/Button";
import { FC } from 'react';

interface pageProps {
  
}

const page: FC<pageProps> = ({}) => {
  return (
  <div>
    <Button />
  </div>)
}

export default page